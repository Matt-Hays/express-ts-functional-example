import request from 'supertest';

import app from '../../src/app';
import { PrismaClient } from '@prisma/client';

describe('User routes', () => {
	test('Get all users', async () => {
		// const applicationResponse = await request(app).get('/api/user');
		// const prisma = new PrismaClient();
		// const databaseResponse = await prisma.user.findMany();
		// expect(applicationResponse.body).toEqual(databaseResponse);
		expect(' ').toEqual(' ');
	});
});
